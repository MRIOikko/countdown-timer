# countdown-timer

A page that counts down the days, hours, minutes and seconds to a date.

The date hardcoded in the JS is one I was counting down to a while back. If you want to use it, open `CounterDay.js` and change `targetDate` to whatever you're waiting for.
